// next.config.js

const nextConfig = {
  // Other Next.js configuration options...
  head: {
    // Add or replace any other meta tags as needed
    link: [
      {
        rel: 'icon',
        href: '/favicon.ico', // Path to your favicon file
      },
    ],
  },
};

export default nextConfig;